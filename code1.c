#include<stdio.h>
int main(){
    int x,y,sum;
    printf("write the value of x:");
    scanf("%d",&x);
printf("write the value of y:");
    scanf("%d",&y);
    sum=x+y;
    printf("The sum is: %d",sum);
    return(0);
}